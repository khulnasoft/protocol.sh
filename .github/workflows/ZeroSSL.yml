name: ZeroSSL
on:
  schedule:
    - cron: '10 9 * * *'
  workflow_dispatch:
  push:
    branches:
      - 'protocoltest'
    paths:
      - '**.sh'
      - '.github/workflows/ZeroSSL.yml'
  pull_request:
    branches:
      - 'protocoltest'
    paths:
      - '**.sh'
      - '.github/workflows/ZeroSSL.yml'



concurrency: 
  group: ${{ github.workflow }}
  cancel-in-progress: true



jobs:
  ZeroSSL:
    runs-on: ubuntu-latest
    env:
      TEST_PROTOCOL_Server: "ZeroSSL.com"
      CA_ECDSA: "ZeroSSL ECC Domain Secure Site CA"
      CA: "ZeroSSL RSA Domain Secure Site CA"
      CA_EMAIL: "githubtest@protocol.sh"
      TEST_PREFERRED_CHAIN: ""
    steps:
    - uses: actions/checkout@v4
    - name: Run Test
      run: |
          sudo apt-get update && sudo apt-get install -y curl socat && \
          cd ../protocol.sh && sudo --preserve-env ./letest.sh


